{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Editar Factura</h1>

{{ form(edit_form) }}
    
        <ul class="record_actions">
    <li>
        <a href="{{ path('factura') }}">
             Volver a Lista
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
